export default function TextMedia(props: any) {
    const {content, image, pos, cta, link} = props;

    return (
        <section>
            <div className="container">
                <div class></div>
            </div>
        </section>
    )
}